{
  "slug": "power_of_make_believe_red",
  "abilities": [
    {
      "ability_type": "WHILE_STATIC",
      "_comment": "Was a plain STATIC, which nothing dispatches -- the ability never ran. The effect was MODIFY_ATTACK_POWER_PER_UNIQUE_AURA, which counts distinct AURA NAMES in the arena. This card counts the cards with 6 or more {p} DEFENDING IT, which is not that. Five cards reached for that effect because there was no way to count what they meant. Its nested condition also asked SELF_ATTACK_POWER_GTE -- the ATTACK's power, not the defender's.",
      "conditions": [
        {
          "type": "SOURCE_IS_ATTACK"
        }
      ],
      "effects": [
        {
          "type": "MODIFY_ATTACK",
          "mod": "add",
          "amount": {
            "type": "COUNT_DEFENDING",
            "power_gte": 6
          }
        }
      ]
    }
  ],
  "_comment": "Implementation copied verbatim from power_of_make_believe_blue, whose printed functional text is identical to this card's once each card's own name is substituted out. Everything that differs between them -- cost, pitch, power, colour -- comes from the card DB, not from this file, so the DSL is the same card twice.\n\nTHE COPY IS PINNED, not trusted: tests/test_copied_card_implementations.py asserts that these abilities still match power_of_make_believe_blue's and that the two printed texts still agree, so a later edit to one cannot silently leave the other behind. That is the insult_to_injury failure -- three printings of one sentence with three different implementations, the blue one missing a gate the others had.",
  "_copied_from": "power_of_make_believe_blue"
}
