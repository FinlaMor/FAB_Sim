{
  "slug": "bask_in_your_own_greatness_yellow",
  "abilities": [
    {
      "ability_type": "TRIGGERED",
      "trigger": "ON_ATTACK",
      "effects": [
        {
          "type": "PAY_UP_TO",
          "max": 3,
          "asset": "RESOURCES"
        },
        {
          "type": "CREATE_TOKEN",
          "token": "might",
          "amount": {
            "type": "PAID_AMOUNT"
          }
        }
      ]
    }
  ],
  "_comment": "Implementation copied verbatim from bask_in_your_own_greatness_red, whose printed functional text is identical to this card's once each card's own name is substituted out. Everything that differs between them -- cost, pitch, power, colour -- comes from the card DB, not from this file, so the DSL is the same card twice.\n\nTHE COPY IS PINNED, not trusted: tests/test_copied_card_implementations.py asserts that these abilities still match bask_in_your_own_greatness_red's and that the two printed texts still agree, so a later edit to one cannot silently leave the other behind. That is the insult_to_injury failure -- three printings of one sentence with three different implementations, the blue one missing a gate the others had.",
  "_copied_from": "bask_in_your_own_greatness_red"
}
